# This file should be encrypted with SOPS before committing.
# Encrypt with: sops --encrypt --age <age-public-key> secret.enc.yaml
#
# Template (DO NOT commit plaintext values):
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: slskd-env
#   namespace: media
# stringData:
#   SLSKD_USERNAME: <username>
#   SLSKD_PASSWORD: <password>
#
# After filling in values, encrypt:
#   sops --encrypt --in-place secret.enc.yaml
